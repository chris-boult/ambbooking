'use client'

export default function CreateBusinessPage() {
  return (
    <main style={{ padding: '40px' }}>
      <h1>Create business</h1>
      <p>This page needs restoring, but the build is now safe.</p>
    </main>
  )
}