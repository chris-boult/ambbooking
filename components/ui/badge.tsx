import {HTMLAttributes} from 'react';import {cn} from '@/lib/cn';export default function Badge({className,...p}:HTMLAttributes<HTMLSpanElement>){return <span {...p} className={cn('inline-flex rounded-full bg-slate-800 px-2 py-1 text-xs',className)}/>}
