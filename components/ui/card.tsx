import {HTMLAttributes} from 'react';import {cn} from '@/lib/cn';export default function Card({className,...p}:HTMLAttributes<HTMLDivElement>){return <div {...p} className={cn('rounded-2xl border border-slate-800 bg-slate-900 p-6',className)}/>}
