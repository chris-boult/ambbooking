import {InputHTMLAttributes} from 'react';import {cn} from '@/lib/cn';export default function Input({className,...p}:InputHTMLAttributes<HTMLInputElement>){return <input {...p} className={cn('w-full rounded-xl border border-slate-700 bg-slate-800 px-3 py-2',className)}/>}
